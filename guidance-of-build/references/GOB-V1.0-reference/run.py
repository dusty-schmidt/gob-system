#this should be the main entry point for the app
#it should start out with a simple device recon process 


#new device?  check for local config file thats finalized upon successful setup
#no config?  generate empty config file at $HOME/.gobconfig/device_config.json and a copy in the GOB codebase.
#config exists?  check for updates
#config exists and is up to date?  run the app

#device recon 
#prompt user for device nickname, username, and dob (all are optional)
#